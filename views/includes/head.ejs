<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />
    <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
  />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <nav class="roboto-medium">
      <h1><a href="/">BLOG.IO</a></h1>
      <div>
        <% if(isLogin) { %>
            <a href="/admin/create-post"
              ><i class="fa-regular fa-square-plus icon"></i>Create Post</a
            >
            <a href="/admin/profile/"><i class="fa-solid fa-circle-user icon"></i> Profile</a>
            <form action="/logout" method="post">
              <input type="text" value="<%= csrfToken %>" name="_csrf" hidden />
              <button class="btn">
                <i class="fa-solid fa-arrow-right-from-bracket icon"></i>Logout
              </button>
            </form>
        <% } else { %>
            <a href="/login"
              ><i class="fa-solid fa-user-check icon"></i>Login to existing account</a
            >
            <a href="/register"
              ><i class="fa-solid fa-user-plus icon"></i>Create account</a
            >
        <% } %>
      </div>
    </nav>
